<UserControl x:Class="JewelryPOS.App.Views.Settings.AppSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JewelryPOS.App.Views.Settings"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Müşteri Ekranı Görseli" FontSize="18" Margin="0,0,0,10"/>
            <Image Source="{Binding CustomerViewImagePath}" Width="200" Height="200" Margin="0,0,0,10"/>
            <Button Content="Görsel Seç" Command="{Binding SelectImageCommand}" Width="100" Height="30"/>
        </StackPanel>
    </Grid>
</UserControl>